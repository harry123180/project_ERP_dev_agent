# Development environment variables
# Network accessible configuration for WiFi access
# Use your computer's IP address for network access
# Use relative path for API - will use same host as frontend
VITE_API_BASE_URL=/api/v1

# Development settings
NODE_ENV=development
VITE_APP_TITLE=ERP System (Development)
VITE_DEBUG=true